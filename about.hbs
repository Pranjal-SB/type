# Third-party licences

TYPE is MIT licensed — see [LICENSE](LICENSE). The binary also contains code from the crates
listed below, and their licences require that their notices travel with it.

**Generated at build time. Not edited, not committed.** Reproduce with:

```
cargo about generate --fail about.hbs -o THIRD-PARTY-LICENSES.md
```

CI runs the same command on every pull request, so a dependency whose licence text cannot be
resolved fails the change that introduced it rather than the release three weeks later.

The MIT and ISC texts repeat below because each one carries its own copyright line, and it is
the copyright line that the licence requires be reproduced.

## Summary

| Licence | Crates |
|---|---|
{{#each overview}}| {{name}} | {{count}} |
{{/each}}

{{#each licenses}}
## {{name}}

Used by {{#each used_by}}{{#if @index}}, {{/if}}[{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}}){{/each}}.

```text
{{text}}
```

{{/each}}
